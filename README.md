# mongoUserManager
run ``npm start`` to run app in production mode. This will clear the users database and then populate it with 10 users.

run ``npm devServe`` to run the app in dev mode. This is the same as prodoction mode except it restarts with every change.
